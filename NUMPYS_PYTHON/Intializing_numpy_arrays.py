# intializing an array of x * y dimensions with 0
import numpy as np
print(np.zeros((3,4)))

# arranging numbers in between x and y with an interval z
import numpy as np
print(np.arange(1,11,2))

# arranging z numbers between x and y
print(np.linspace(1,2,10))

# filling same number in a array of "x" X"y"
print(np .full((2,3),6))

# filling random number in a array of "x" X"y"
print(np.random.random((5,2)))